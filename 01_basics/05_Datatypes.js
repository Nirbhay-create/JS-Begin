
// cant use alert in machine ide node it need to br in inside doc which we do in 
//browser or with html document
let a=10
let b=10n;
let c="sd"
let d;
let e=true
let f=undefined;
let g=null;
console.table([typeof(a),typeof(b),typeof(c),typeof(d),typeof(e),typeof(f),typeof(g)]);
console.log((3**2)**2);